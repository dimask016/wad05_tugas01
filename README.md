Technical Assignment: Web Security & JavaScript
DOM Manipulation
1. Explain the differences between innerHTML and textContent, when each should be
used, and provide examples for both.
2. Provide a real-world scenario where Cross-Site Scripting (XSS) can be exploited if user
input is not validated or escaped properly. Include a description of the vulnerability, the
exploitation process, and its impact.
3. Create a standalone HTML+JS file featuring a live search input that filters a data array in
real time (using input event + array.filter + re-render). The implementation must
strictly use createElement and textContent—the use of innerHTML is strictly
prohibited
